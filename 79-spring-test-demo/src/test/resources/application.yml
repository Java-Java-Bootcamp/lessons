spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  sql:
    init:
      mode: embedded
      schema-locations: classpath*:db/schema.sql
  jpa:
    show-sql: true
    open-in-view: true
